<li>
  <%= gravatar_for company, size: 50 %>
  <%= link_to company.name, company %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", company, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>
