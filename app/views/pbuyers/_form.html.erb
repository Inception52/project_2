<%= form_with(model: pbuyer, local: true) do |form| %>
  <% if pbuyer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pbuyer.errors.count, "error") %> prohibited this pbuyer from being saved:</h2>

      <ul>
      <% pbuyer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references %>
  </div>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
