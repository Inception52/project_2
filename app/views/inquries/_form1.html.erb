<%=form_tag(reply_inqury_path, method: "post") do%>
  <% if inqury.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inqury.errors.count, "error") %> prohibited this inqury from being saved:</h2>

      <ul>
        <% inqury.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reply %>
    <%= form.text_field :reply %>
  </div>

  <div class="actions">
    <%= form.submit(:pid => @pid) %>
  </div>
<% end %>